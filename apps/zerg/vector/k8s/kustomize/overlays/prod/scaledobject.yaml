apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: zerg-vector
  labels:
    app: zerg-vector
spec:
  scaleTargetRef:
    name: zerg-vector
  minReplicaCount: 0          # Scale to zero when idle
  maxReplicaCount: 10
  pollingInterval: 15
  cooldownPeriod: 300
  triggers:
    # Prometheus: scale based on gRPC request rate
    - type: prometheus
      metadata:
        serverAddress: http://prometheus.monitoring.svc.cluster.local:9090
        query: sum(rate(grpc_server_handled_total{grpc_service=~".*Vector.*"}[2m]))
        threshold: "10"           # Scale up when > 10 req/sec
        activationThreshold: "1"  # Wake from zero when > 1 req/sec
    # Fallback: CPU-based scaling (only works when replicas > 0)
    - type: cpu
      metricType: Utilization
      metadata:
        value: "70"
  advanced:
    horizontalPodAutoscalerConfig:
      behavior:
        scaleDown:
          stabilizationWindowSeconds: 300
          policies:
            - type: Percent
              value: 10
              periodSeconds: 60
        scaleUp:
          stabilizationWindowSeconds: 0
          policies:
            - type: Percent
              value: 100
              periodSeconds: 15
            - type: Pods
              value: 4
              periodSeconds: 15
          selectPolicy: Max
