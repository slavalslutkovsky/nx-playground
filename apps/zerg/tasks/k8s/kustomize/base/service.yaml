apiVersion: v1
kind: Service
metadata:
  name: zerg-tasks
spec:
  selector:
    app: zerg-tasks
  ports:
    - protocol: TCP
      name: grpc
      port: 50051
      targetPort: 50051
  type: ClusterIP
