{
  "name": "ci-manager-api",
  "version": "0.0.1",
  "description": "CI Pipeline Manager API",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "lint": "biome check --write ."
  },
  "dependencies": {
    "hono": "^4.7.10"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.2"
  }
}
