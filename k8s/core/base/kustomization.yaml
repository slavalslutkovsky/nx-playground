apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - configmaps/shared-config.yaml
  # Note: secrets should be created manually or via sealed-secrets/external-secrets
  # - secrets/shared-secrets.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: zerg
      app.kubernetes.io/managed-by: kustomize
