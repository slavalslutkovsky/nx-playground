apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: arangodb-data
  name: arangodb-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
