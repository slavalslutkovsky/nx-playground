apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: milvus-data
  name: milvus-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
